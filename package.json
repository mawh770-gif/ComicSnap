{
  "name": "comicscanpro-client",
  "version": "1.2.0",
  "description": "The professional client application for fetching and managing comic metadata via scanning and Firebase Cloud Functions.",
  "main": "index.html",
  "scripts": {
    "build": "bash ./build.sh",
    "start": "npm run build && firebase serve",
    "deploy": "npm run build && firebase deploy"
  },
  "keywords": [
    "firebase",
    "auth",
    "google-signin",
    "modular",
    "comicscanpro"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^11.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}